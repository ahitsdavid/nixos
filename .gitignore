# Claude Code project file (local only)
CLAUDE.md

# Age keys - NEVER COMMIT YOUR PRIVATE KEYS!
**/*age/keys.txt
**/sops/age/keys.txt

# Backup files that sops might create
secrets/*.yaml.bak
secrets/.*.yaml.swp

# NOTE: Encrypted .yaml files in secrets/ are SAFE to commit
# SOPS encrypts them, so they should be tracked in git

# Nix build results - ISO files can be large
result
result-*
*.iso